name: Lint - duplication HTML (thematique, fictions, petitfablab, ccn)

on:
  pull_request:
    paths:
      - 'plugins/projets/thematique/**/*.html'
      - 'plugins/projets/fictions/**/*.html'
      - 'plugins/projets/petitfablab/**/*.html'
      - 'plugins/projets/ccn/**/*.html'
      - '.ci/check_html_duplication.js'
      - '.ci/html-duplication-baseline.txt'
      - 'package.json'
      - 'package-lock.json'

jobs:
  check-html-duplication:
    # Pas de déclencheur push (dev/main) : build_dev.yml fait déjà tourner ce
    # même check (job html_duplication) à chaque push sur dev - éviter le
    # doublon en parallèle. main ne reçoit que des merges de PR (déjà
    # couverts par le déclencheur pull_request ci-dessus).
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Installer les dépendances npm
        run: npm ci

      - name: Vérifier l'absence de nouvelle duplication HTML
        run: node .ci/check_html_duplication.js
